import 'package:flutter/material.dart';

class BecameSallerWidget extends StatelessWidget {
  const BecameSallerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(10),
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}
